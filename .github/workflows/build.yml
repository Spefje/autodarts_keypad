name: Build Firmware

on:
  push:
    paths:
      - ".github/workflows/**"
      - "config/**"
      - "build.yaml"
  workflow_dispatch:

jobs:
  build:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@main
