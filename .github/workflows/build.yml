name: Build Firmware

on:
  push:
    paths:
      - ".github/workflows/**"
      - "config/**"
      - "build.yaml"

jobs:
  build:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@main
